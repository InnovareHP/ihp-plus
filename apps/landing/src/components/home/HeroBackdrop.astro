<div class="backdrop" aria-hidden="true">
  <div class="backdrop-layer" data-depth="30">
    <span class="aurora aurora-blue"></span>
    <span class="aurora aurora-mint"></span>
    <span class="aurora aurora-deep"></span>
  </div>
  <span class="sweep"></span>
</div>

<style>
  /* Fades out before the panel's foot, so the bulge below never shows a colour seam. */
  .backdrop {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    mask-image: linear-gradient(to bottom, #000 55%, transparent 92%);
  }
  .backdrop-layer {
    position: absolute;
    inset: -40px;
  }

  .aurora {
    position: absolute;
    border-radius: 9999px;
    mix-blend-mode: screen;
    opacity: 0.55;
    will-change: transform;
  }
  .aurora-blue {
    top: -20%;
    left: -15%;
    width: 60vmax;
    height: 60vmax;
    background: radial-gradient(circle, var(--color-brand-500), transparent 62%);
    animation: drift-a 22s ease-in-out infinite alternate;
  }
  .aurora-mint {
    top: -10%;
    right: -20%;
    width: 50vmax;
    height: 50vmax;
    background: radial-gradient(circle, var(--color-mint-300), transparent 60%);
    opacity: 0.4;
    animation: drift-b 26s ease-in-out infinite alternate;
  }
  .aurora-deep {
    bottom: -25%;
    left: 20%;
    width: 55vmax;
    height: 55vmax;
    background: radial-gradient(circle, var(--color-brand-600), transparent 60%);
    animation: drift-c 30s ease-in-out infinite alternate;
  }

  .sweep {
    position: absolute;
    inset: -50% auto -50% -60%;
    width: 40%;
    background: linear-gradient(
      100deg,
      transparent,
      oklch(1 0 0 / 0.08) 45%,
      oklch(1 0 0 / 0.14) 50%,
      oklch(1 0 0 / 0.08) 55%,
      transparent
    );
    rotate: 12deg;
    animation: sweep 9s ease-in-out 2.5s infinite;
  }

  @keyframes drift-a {
    to {
      transform: translate(18vw, 12vh) scale(1.15);
    }
  }
  @keyframes drift-b {
    to {
      transform: translate(-16vw, 18vh) scale(0.85);
    }
  }
  @keyframes drift-c {
    to {
      transform: translate(14vw, -14vh) scale(1.2);
    }
  }
  @keyframes sweep {
    0% {
      transform: translateX(0);
    }
    45%,
    100% {
      transform: translateX(420%);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .aurora,
    .sweep {
      animation: none;
    }
    .sweep {
      display: none;
    }
  }
</style>
