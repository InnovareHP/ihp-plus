// A short control still needs a full-width label and help text, so cap the input, not the wrapper.
export const NARROW_INPUT = { input: { maxWidth: '15rem' } }

export const TINY_INPUT = { input: { maxWidth: '8rem' } }
